/* eslint-disable prettier/prettier */
const AuthRoutes = {
  dashboard: '/dashboard',
};

/** Non Authenticated URL Routes */
const NonAuthRoutes = {
  landingPage: '/',
};

export { AuthRoutes, NonAuthRoutes };
